# 🎭 Настройка MCP Playwright - Пошаговая инструкция

## ✅ Что уже сделано

1. ✅ Playwright установлен локально в проект (`package.json` и `node_modules`)
2. ✅ Создана локальная конфигурация (`.mcp/playwright-config.json`)
3. ✅ Обновлена документация проекта

---

## ⚠️ Что нужно сделать ВАМ

Чтобы Claude Code мог использовать MCP Playwright для автоматического тестирования, нужно добавить конфигурацию в Claude Desktop.

### Шаг 1: Найти файл конфигурации Claude Desktop

**macOS:**
```bash
~/Library/Application Support/Claude/claude_desktop_config.json
```

**Windows:**
```
%APPDATA%\Claude\claude_desktop_config.json
```

**Linux:**
```
~/.config/Claude/claude_desktop_config.json
```

### Шаг 2: Открыть файл в редакторе

**macOS (через Terminal):**
```bash
# Вариант 1: nano
nano ~/Library/Application\ Support/Claude/claude_desktop_config.json

# Вариант 2: VS Code
code ~/Library/Application\ Support/Claude/claude_desktop_config.json

# Вариант 3: любой текстовый редактор
open -a "TextEdit" ~/Library/Application\ Support/Claude/claude_desktop_config.json
```

### Шаг 3: Добавить конфигурацию Playwright

**Если файл пустой или содержит только `{}`:**

```json
{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp-server"]
    },
    "context7": {
      "command": "npx",
      "args": ["-y", "@context7/mcp-server"]
    }
  }
}
```

**Если уже есть другие MCP серверы:**

Добавьте только секцию `"playwright"` внутрь существующего `"mcpServers"`:

```json
{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp-server"]
    },
    "context7": {
      ...existing config...
    },
    ...other servers...
  }
}
```

### Шаг 4: Сохранить файл

- **nano:** `Ctrl+O` (сохранить), затем `Enter`, затем `Ctrl+X` (выход)
- **VS Code:** `Cmd+S` (macOS) или `Ctrl+S` (Windows/Linux)
- **TextEdit:** `Cmd+S`

### Шаг 5: Перезапустить Claude Desktop

**⚠️ ВАЖНО:** Нужно полностью закрыть и перезапустить:

**macOS:**
1. `Cmd+Q` чтобы полностью закрыть Claude Desktop
2. Открыть Claude Desktop заново

**Windows:**
1. Закрыть все окна Claude Desktop
2. Проверить в Task Manager что процесс `Claude.exe` завершён
3. Открыть Claude Desktop заново

### Шаг 6: Проверить установку

После перезапуска Claude Desktop, вернитесь в этот чат и спросите:

```
Проверь доступен ли MCP Playwright
```

Вы должны увидеть что доступны инструменты:
- ✅ `mcp__playwright__navigate`
- ✅ `mcp__playwright__screenshot`
- ✅ `mcp__playwright__click`
- ✅ `mcp__playwright__fill`
- ✅ `mcp__playwright__console`
- ✅ `mcp__playwright__evaluate`

---

## 🚀 Использование после настройки

### Запуск dev сервера

Перед тестированием запустите dev сервер:

```bash
cd frontend
npm run dev
```

Или запустите все сервисы:

```bash
./start-dev.sh
```

### Примеры команд для Claude Code

**Базовая проверка:**
```
Открой http://localhost:5173 через Playwright и проверь что HomePage отображается корректно
```

**Тестирование с деталями:**
```
Открой http://localhost:5173 через Playwright и:
1. Сделай скриншот HomePage
2. Проверь консоль на ошибки
3. Проверь localStorage (auth данные)
4. Проверь что кнопки "Примерка" и "Редактирование" видны
```

**Тестирование навигации:**
```
Открой HomePage через Playwright, кликни на "Примерка" и проверь что открылся FittingPage
```

**Диагностика ошибок:**
```
Я вижу ошибку при открытии приложения.
Открой через Playwright и диагностируй:
1. Что показывает консоль браузера
2. Что в localStorage
3. Какой компонент отображается
```

---

## 🔧 Troubleshooting

### Проблема: MCP Playwright не доступен после настройки

**Решение:**
1. Проверьте что JSON валидный (используйте JSONLint.com)
2. Убедитесь что путь к файлу правильный
3. **Полностью** перезапустите Claude Desktop (не просто закрыть окно)
4. Проверьте логи Claude Desktop (если доступны)

### Проблема: "command not found: npx"

**Решение:**
Установите Node.js:

```bash
# macOS (через Homebrew)
brew install node

# Проверка
node --version
npm --version
```

### Проблема: Playwright не может открыть браузер

**Решение:**
Установите браузеры Playwright:

```bash
npx playwright install
```

### Проблема: Dev сервер не запускается

**Решение:**

```bash
cd frontend
npm install
npm run dev
```

Проверьте что сервер работает:
```bash
curl http://localhost:5173
# Должен вернуть HTML
```

---

## 📚 Дополнительная информация

**Подробная документация:** `docs/MCP_PLAYWRIGHT_SETUP.md`

**Возможности MCP Playwright:**
- 🌐 Автоматическое открытие в браузере
- 📸 Скриншоты для анализа
- 🖱️ Взаимодействие с UI
- 📋 Чтение консоли и ошибок
- 🔍 Проверка localStorage/cookies
- ✅ Полный E2E flow testing

---

## ✅ Чек-лист

- [x] ✅ Playwright установлен локально
- [x] ✅ Конфигурация создана (`.mcp/playwright-config.json`)
- [x] ✅ Документация обновлена
- [ ] ⚙️ Claude Desktop config обновлён (ВАША ЗАДАЧА)
- [ ] 🔄 Claude Desktop перезапущен (ВАША ЗАДАЧА)
- [ ] ✅ MCP Playwright доступен (проверка)
- [ ] 🚀 Dev сервер запущен
- [ ] 🧪 Первый тест выполнен

---

## 🎯 Следующий шаг

**После выполнения всех шагов**, вернитесь в чат с Claude Code и спросите:

```
Проверь доступен ли MCP Playwright и если да -
открой http://localhost:5173 и протестируй HomePage
```

Удачи! 🚀
