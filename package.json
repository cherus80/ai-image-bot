{
  "name": "ai-media-generator",
  "version": "0.14.0",
  "description": "AI Generator - Telegram Web App for AI image generation (Root Package)",
  "private": true,
  "scripts": {
    "mcp:playwright": "npx -y @playwright/mcp-server",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && pip install -r requirements.txt",
    "dev": "./start-dev.sh",
    "test:frontend": "cd frontend && npm test",
    "test:backend": "cd backend && pytest tests/ -v"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  },
  "keywords": [
    "telegram",
    "webapp",
    "ai",
    "image-generation",
    "fastapi",
    "react"
  ],
  "author": "",
  "license": "MIT"
}
