# 🌐 Настройка туннеля для Telegram Web App

У вас в Claude Code настроен HTTP прокси, который мешает ngrok. Вот несколько решений:

---

## ✅ Решение 1: localtunnel (РЕКОМЕНДУЕТСЯ - уже установлен)

**localtunnel** - бесплатная альтернатива ngrok без ограничений.

### Запуск:

В **новом терминале**:

```bash
npx localtunnel --port 5174
```

Вы увидите:
```
your url is: https://random-name-123.loca.lt
```

**📋 Скопируйте этот URL!**

⚠️ **ВАЖНО:** При первом открытии localtunnel покажет страницу с предупреждением. Нажмите "Continue" для продолжения.

---

## ✅ Решение 2: ngrok БЕЗ прокси

Запустите ngrok с временным отключением прокси:

```bash
unset HTTP_PROXY HTTPS_PROXY http_proxy https_proxy && ngrok http 5174
```

Это отключит прокси только для этой команды.

---

## ✅ Решение 3: Отключить прокси в Claude Code

Отредактируйте [.claude/settings.json](.claude/settings.json) и временно закомментируйте секцию `env`:

```json
{
  "permissions": { ... },
  // Временно закомментируйте:
  // "env": {
  //   "HTTP_PROXY": "...",
  //   "HTTPS_PROXY": "...",
  //   ...
  // }
}
```

Затем перезапустите Claude Code и используйте обычный ngrok.

⚠️ **Не забудьте вернуть прокси обратно после тестирования!**

---

## 📋 Дальнейшие шаги (после запуска туннеля)

1. **Скопируйте HTTPS URL** от localtunnel/ngrok

2. **Обновите .env:**
   ```env
   WEB_APP_URL=https://ваш-tunnel-url
   ```

3. **Настройте в @BotFather:**
   - `/mybots` → ваш бот → Bot Settings → Menu Button → Configure menu button
   - Отправьте ваш tunnel URL

4. **Запустите бота:**
   ```bash
   ./start-local-telegram.sh
   ```

5. **Откройте в Telegram:**
   - Найдите бота → `/start` → "🚀 Открыть App"

---

## 🎯 Быстрый старт с localtunnel

```bash
# 1. Запустите туннель (в новом терминале)
npx localtunnel --port 5174

# 2. Скопируйте URL (например: https://random-123.loca.lt)

# 3. Обновите .env
# WEB_APP_URL=https://random-123.loca.lt

# 4. Настройте @BotFather (см. выше)

# 5. Запустите бота
./start-local-telegram.sh
```

---

## 🔍 Проверка туннеля

После запуска туннеля проверьте, что он работает:

```bash
# Откройте URL в браузере
open https://ваш-tunnel-url

# Или через curl
curl https://ваш-tunnel-url
```

Должен открыться ваш frontend.

---

## ⚠️ Особенности localtunnel

1. **Первый визит:** покажет страницу "Tunnel Password". Нажмите "Continue"
2. **URL меняется:** при каждом перезапуске (как и ngrok)
3. **Бесплатно:** без ограничений, работает с прокси

---

## 🛑 Остановка туннеля

```bash
# В терминале с туннелем нажмите:
Ctrl+C
```

---

Готово! Выберите удобный для вас вариант и продолжайте настройку 🚀
