# MCP Playwright Setup Guide

Этот guide объясняет как настроить MCP Playwright для автоматического тестирования фронтенда через Claude Code.

---

## Что такое MCP Playwright?

MCP Playwright - это Model Context Protocol сервер, который позволяет Claude Code:
- 🌐 Автоматически открывать приложение в браузере
- 📸 Делать скриншоты страниц
- 🖱️ Взаимодействовать с элементами (клики, ввод текста)
- 📋 Читать консоль браузера и перехватывать ошибки
- 🔍 Проверять localStorage, cookies, network requests
- ✅ Тестировать весь UI flow автоматически

---

## Установка

### ✅ Шаг 1: Локальная установка (Уже выполнено!)

Playwright уже установлен локально в этом проекте:

```bash
# В корне проекта уже есть:
- package.json (с @playwright/test)
- node_modules/@playwright/test
- .mcp/playwright-config.json
```

### ⚙️ Шаг 2: Настройка Claude Desktop

Чтобы Claude Code мог использовать MCP Playwright, нужно добавить конфигурацию в Claude Desktop.

#### macOS:

1. **Найти файл конфигурации:**
   ```bash
   ~/Library/Application Support/Claude/claude_desktop_config.json
   ```

2. **Открыть файл в редакторе:**
   ```bash
   nano ~/Library/Application\ Support/Claude/claude_desktop_config.json
   # или
   code ~/Library/Application\ Support/Claude/claude_desktop_config.json
   ```

3. **Добавить конфигурацию Playwright:**

   Если файл пустой или содержит только `{}`:
   ```json
   {
     "mcpServers": {
       "playwright": {
         "command": "npx",
         "args": ["-y", "@playwright/mcp-server"]
       },
       "context7": {
         "command": "npx",
         "args": ["-y", "@context7/mcp-server"]
       }
     }
   }
   ```

   Если уже есть другие MCP серверы:
   ```json
   {
     "mcpServers": {
       "playwright": {
         "command": "npx",
         "args": ["-y", "@playwright/mcp-server"]
       },
       "context7": {
         ...existing config...
       },
       ...other servers...
     }
   }
   ```

4. **Сохранить файл:**
   - Для nano: `Ctrl+O`, затем `Enter`, затем `Ctrl+X`
   - Для VS Code: `Cmd+S`

#### Windows:

```
%APPDATA%\Claude\claude_desktop_config.json
```

#### Linux:

```
~/.config/Claude/claude_desktop_config.json
```

### 🔄 Шаг 3: Перезапустить Claude Desktop

**ВАЖНО:** После изменения конфигурации **ОБЯЗАТЕЛЬНО** перезапустить Claude Desktop:

1. Полностью закрыть Claude Desktop (`Cmd+Q` на macOS)
2. Открыть заново

---

## Проверка установки

После перезапуска Claude Desktop, попросите Claude Code:

```
Проверь доступен ли MCP Playwright
```

Должны появиться инструменты:
- ✅ `mcp__playwright__navigate`
- ✅ `mcp__playwright__screenshot`
- ✅ `mcp__playwright__click`
- ✅ `mcp__playwright__fill`
- ✅ `mcp__playwright__console`
- ✅ `mcp__playwright__evaluate`

---

## Использование

### Базовый workflow:

```bash
# 1. Запустить dev сервер (если не запущен)
npm run dev
# или
./start-dev.sh

# 2. Попросить Claude Code протестировать:
"Открой http://localhost:5173 через Playwright и проверь что HomePage отображается корректно"

# 3. Claude Code автоматически:
- Откроет браузер
- Сделает скриншот
- Проверит консоль на ошибки
- Проверит localStorage
- Предоставит детальный отчёт
```

### Примеры команд для Claude Code:

**Базовая проверка страницы:**
```
Открой http://localhost:5173 через Playwright, сделай скриншот и проверь консоль
```

**Тестирование навигации:**
```
Открой HomePage через Playwright, кликни на кнопку "Примерка" и проверь что открылся FittingPage
```

**Проверка авторизации:**
```
Открой приложение через Playwright и проверь:
1. Что AuthGuard работает в DEV режиме
2. Что localStorage содержит корректные mock данные
3. Что нет ошибок в консоли
```

**Тестирование формы:**
```
Открой FittingPage через Playwright и протестируй:
1. Загрузку фото пользователя
2. Переход к следующему шагу
3. Загрузку фото предмета
4. Выбор зоны
```

---

## Доступные инструменты Playwright

### 1. `mcp__playwright__navigate`
Открыть URL в браузере.

**Пример:**
```
navigate to http://localhost:5173
```

### 2. `mcp__playwright__screenshot`
Сделать скриншот текущей страницы.

**Пример:**
```
take screenshot
```

### 3. `mcp__playwright__click`
Клик по элементу.

**Пример:**
```
click button:has-text("Примерка")
```

### 4. `mcp__playwright__fill`
Заполнить форму.

**Пример:**
```
fill input[name="email"] with "test@example.com"
```

### 5. `mcp__playwright__console`
Прочитать логи консоли браузера.

**Пример:**
```
read console logs
```

### 6. `mcp__playwright__evaluate`
Выполнить JavaScript в контексте страницы.

**Пример:**
```javascript
// Проверить localStorage
const auth = localStorage.getItem('auth-storage');
return JSON.parse(auth);
```

---

## Типичные сценарии тестирования

### Сценарий 1: Первый запуск приложения

```
Claude, открой http://localhost:5173 через Playwright и проверь:
1. Что HomePage отображается корректно
2. Что кнопки "Примерка" и "Редактирование" видны
3. Что нет ошибок в консоли
4. Что localStorage содержит mock данные авторизации
```

### Сценарий 2: Диагностика ошибки

```
Claude, я вижу ошибку "Something went wrong" при открытии приложения.
Открой приложение через Playwright и диагностируй:
1. Какую именно ошибку показывает консоль
2. Что находится в localStorage
3. Какой статус у API запросов
4. Что показывает ErrorPage
```

### Сценарий 3: Тестирование UI flow

```
Claude, протестируй весь flow примерки через Playwright:
1. Открой HomePage
2. Кликни "Примерка"
3. Проверь что открылся Step 1 (загрузка фото)
4. Заполни форму (если возможно)
5. Перейди к Step 2
6. Проверь навигацию между шагами
7. Сделай скриншоты каждого шага
```

### Сценарий 4: Проверка адаптивности

```
Claude, открой приложение через Playwright на разных размерах экрана:
1. Desktop (1920x1080)
2. Tablet (768x1024)
3. Mobile (375x667)
Сделай скриншоты и проверь что UI корректно адаптируется
```

---

## Troubleshooting

### Проблема: MCP Playwright не доступен после настройки

**Решение:**
1. Убедитесь что конфигурация JSON валидна (используйте JSONLint)
2. Проверьте что путь к файлу конфигурации правильный
3. **Полностью** перезапустите Claude Desktop (`Cmd+Q`, потом открыть заново)
4. Проверьте логи Claude Desktop (если доступны)

### Проблема: "command not found: npx"

**Решение:**
Установите Node.js:
```bash
# macOS (через Homebrew)
brew install node

# Или скачайте с https://nodejs.org/
```

### Проблема: Playwright не может открыть браузер

**Решение:**
Установите браузеры Playwright:
```bash
npx playwright install
```

### Проблема: Dev сервер не запускается

**Решение:**
```bash
cd frontend
npm install
npm run dev
```

Проверьте что сервер работает:
```bash
curl http://localhost:5173
```

---

## Интеграция с CI/CD

В будущем можно добавить автоматические E2E тесты через Playwright:

```bash
# playwright.config.ts
export default defineConfig({
  testDir: './tests/e2e',
  use: {
    baseURL: 'http://localhost:5173',
  },
  webServer: {
    command: 'npm run dev',
    port: 5173,
  },
});
```

---

## Дополнительные ресурсы

- **Playwright Documentation:** https://playwright.dev/
- **MCP Protocol:** https://modelcontextprotocol.io/
- **Claude Code Docs:** https://docs.claude.com/claude-code
- **Playwright MCP Server:** https://github.com/playwright/mcp-server

---

## Чек-лист установки

- [x] ✅ Playwright установлен локально (`package.json`)
- [ ] ⚙️ Claude Desktop config обновлён
- [ ] 🔄 Claude Desktop перезапущен
- [ ] ✅ MCP Playwright доступен (проверка через Claude Code)
- [ ] 🚀 Dev сервер запущен (`npm run dev`)
- [ ] 🧪 Первый тест выполнен успешно

---

**Следующий шаг:** После выполнения всех шагов, попросите Claude Code открыть и протестировать ваше приложение!

```
Claude, открой http://localhost:5173 через Playwright и проверь что всё работает корректно
```
